# 窥岛 · Peep Island — 项目运行说明

代码仓库：https://github.com/raccoonBK/peep-island

---

## 零、三分钟看到核心（推荐评审按这条走）

窥岛的核心体验是**第四面墙**——角色让你知道她一直在看着你。正常流程需要亲密度 50 且真实等待 24 小时，演示等不起，因此项目内置了演示入口。

启动之后（见第二节），按以下顺序：

1. 浏览器打开 http://localhost:3711 ，看到岛屿主界面：小人按作息表站在各自的位置，头顶冒气泡（❗烦恼 / ✉️未读 / 💤睡着 / 🍽没喂）
2. 点任意一个小人 → 弹出角色卡（幸福/亲密双条、烦恼、关系、「TA眼中的你」）
3. 点「说话」进聊天，随便说一句 —— 注意她**不会秒回**：延迟 8~88 秒送达，"正在输入"只在到达前 7 秒才出现
4. 点「👀 看看岛上」→ 生成一场两个 NPC 之间的小剧场，字幕条逐拍播出，你只是观众
5. **看第四面墙**：新开一个标签页访问
   ```
   http://localhost:3711/api/rooms/ache/demo-crack
   ```
   页面会返回一段 JSON（就是刚生成的台词）。然后**回到游戏标签页，点开阿澈的聊天**——整屏变黑，白字逐字浮现。

> 这条演示入口走的是完整的三层约束生成流程，看到的就是实机效果，不是预设脚本。返回 JSON 里的 `src` 字段标明来源：`ai:1` = 生成一次通过判别，`ai:2` = 重 roll 后通过，`fallback:*` = 判别未过已降级到兜底库。
> 它不会改变角色真实的第四面墙进度。

把 `ache` 换成 `xiaobei` 或 `yuanzi` 可以看另外两个角色的版本——每一句都不一样，因为台词锚定的是数据库里属于你的真实数据。

---

## 一、环境要求

- **Node.js 20.6 或以上**（项目用 Node 原生的 `--env-file`，不依赖 dotenv）
- 无需数据库服务：SQLite 是单文件，随项目自动创建
- 无需 GPU、无需 Docker
- **不填任何 API key 也能完整跑通**，此时角色走脚本托底模式，全部流程可体验（UI 会明确标注「脚本」，不伪装成 AI）

## 二、启动

```bash
git clone https://github.com/raccoonBK/peep-island.git
cd peep-island

# 1) 构建前端
cd client
npm install
npm run build

# 2) 启动服务端（会同时托管刚构建好的前端）
cd ../server
npm install
cp .env.example .env        # 按需填 key，见第三节
npm start
```

打开 **http://localhost:3711** 。

### 开发模式（前端热更新）

```bash
cd client && npm run dev     # http://localhost:5173
```
服务端仍需在另一个终端跑着（`cd server && npm start`）。

### macOS 一键启动

双击项目根目录的 **`窥岛.command`**，自动启动服务端并打开浏览器。
（修改前端代码后需要重新 `cd client && npm run build` 才会反映到 3711 端口）

## 三、配置 API Key

编辑 `server/.env`，四家任填其一即可，**不必都填**：

```ini
AI_PROVIDER=deepseek        # 默认引擎：claude | deepseek | kimi | gemini
                            # 不填则自动选第一个有 key 的

ANTHROPIC_API_KEY=
DEEPSEEK_API_KEY=
MOONSHOT_API_KEY=           # Kimi（月之暗面）
GEMINI_API_KEY=

DAILY_QUOTA=5               # 每角色每日真实 AI 调用上限，超出走脚本托底
CATCHUP_HOURS=4             # 离岛多久触发"离岛报告"
```

运行时还可以在界面右上角 **⚙️** 里热切换引擎，无需重启。
聊天框输入 `//你的话` 会把同一句话同时发给所有已配置的引擎，横向对比各家的"人味"。

## 四、常见问题

**Q：启动报 `better-sqlite3` 构建失败**
A：这是原生模块。确认 Node ≥ 20.6，然后 `cd server && npm rebuild better-sqlite3`。macOS 需要已安装 Xcode Command Line Tools（`xcode-select --install`）。

**Q：角色不回复 / 一直显示脚本托底**
A：三种可能——① `.env` 里没有可用 key；② 今日配额用完（调大 `DAILY_QUOTA`）；③ 网络无法访问模型 API。界面 ⚙️ 面板会显示最近一次错误原因。

**Q：破壁演出没有出现**
A：正常流程需要亲密度 ≥50 且距上一状态满 24 小时真实时间（防止改系统时间加速）。演示请用第零节的入口。

**Q：中文路径下开库失败**
A：已修复。项目用 `fileURLToPath` 而非 `URL.pathname` 打开数据库，中文目录名可正常工作。

**Q：裂缝演出的字体不是像素风**
A：中文像素字体无法通过系统字体栈获得，需要嵌入字体文件。把任意中文像素字体（推荐 Zpix，OFL 授权可商用）命名为 `zpix.woff2` 放进 `client/public/fonts/` 即自动生效；文件不存在时会静默回落到等宽字体，不报错。

## 五、目录结构

```
server/
  db.js        SQLite schema + 角色种子 + 迁移
  brain.js     prompt 组装 / 记忆检索 / 每日配额 / 第四面墙状态机 /
               裂缝台词三层约束生成 / 四引擎适配
  fallback.js  脚本托底台词
  index.js     API 路由、惰性生成、静态托管
client/
  src/App.jsx     手机壳 UI（岛 / 聊天 / 动态 / 设置）
  src/Avatar.jsx  参数化角色渲染器（SVG 分层）
  src/Scene.jsx   参数化岛屿渲染器
  public/         美术资源投放位（tiles / props / buildings / fonts）
BP.md          商业计划书
README.md      作品介绍与设计说明
```

## 六、数据与隐私

- 全部数据存放在 `server/island.db`（本地单文件），不上传任何服务器
- 每日自动备份到 `server/backups/`
- API key 只存在于本地 `server/.env`，已在 `.gitignore` 中排除，不会进入仓库
